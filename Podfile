# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Calendar_FS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
   pod 'FSCalendar'


  # Pods for Calendar_FS

  target 'Calendar_FSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Calendar_FSUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
